# -*- mode: snippet; -*-
# name: action
# --
define([
    'actions/action'
], function(Action){
    var $1 = Action.extend({
        initialize: function(){
            Action.prototype.initialize.apply(this, arguments);
        },
        start: function(){
            $0
        }
    });

    return $1;
});
